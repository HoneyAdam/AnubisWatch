apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: anubiswatch-data
  namespace: anubiswatch
  labels:
    app.kubernetes.io/name: anubiswatch
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
